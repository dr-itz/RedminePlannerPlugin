# == Schema Information
#
# Table name: plan_configs
#
#  id         :integer          not null, primary key
#  project_id :integer          default(0), not null
#  config     :string(255)      not null
#

one:
  id: 1
  project_id: 2
  config: <%= { 'graph_weeks' => '10', 'graph_weeks_past' => '3' }.to_yaml.inspect %>
