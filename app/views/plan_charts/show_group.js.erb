jQuery('#group-chart-display').html('<%= escape_javascript(
  render :partial => 'chart', :locals => {
    :chart => @chart, :chart_name => 'chartGroup', :legend_partial => 'legend_group'}) %>');
jQuery('#week_start_date').val('<%= @chart.start_date.to_s %>');
