<h2><%= l(:label_planner_task_edit) %></h2>

<%= labelled_form_for(@plan_task) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_save) %>
<% end %>

<%= link_to l(:label_planner_tasks), project_plan_tasks_path(@project) %>
